#pragma once

#include <cstdint>
#include <cstddef>

void run_project_1(float lambda,
                   std::size_t max_queue_customers,
                   std::size_t customers_to_serve,
                   std::size_t L);
